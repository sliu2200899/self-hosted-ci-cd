# Runtime dependencies.
#
# Bounded rather than exactly pinned so a fresh resolve cannot fail on a yanked
# release. See doc/02-app.md for how to move to an exact lock file once the
# pipeline is green.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
